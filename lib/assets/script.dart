void main() async {
  // print('Refreshing data...');
  // final data = await decodeCompressedData(fileName: 'places.json');
  // File('places.json').writeAsStringSync(jsonEncode(data));
  // final newPlaces = await File('new.json').readAsString();
  // final updatedPlaces = (jsonDecode(newPlaces) as List).map((e) {
  //   return Place.fromGeoDB(e);
  // }).toList();
  // data.addAll(updatedPlaces);
  // final finalData = jsonEncode(data);
  // File('places.json').writeAsStringSync(finalData);
  // refreshData(fileName: 'places.json');
  // print('Done');
}
